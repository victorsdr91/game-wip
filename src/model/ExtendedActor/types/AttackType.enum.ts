export enum AttackType {
    MAGICAL = "magical",
    PHYSICAL = "physical"
}

export enum ElementType {
    NORMAL = "normal",
    FIRE = "fire",
    WATER = "water",
    LIGHT = "light",
    SHADOW = "shadow",
}

export enum DamageType {
    SINGLE = "single",
    AREA = "area",
}