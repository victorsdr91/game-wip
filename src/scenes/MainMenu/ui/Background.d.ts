import { ScreenElement } from "excalibur";
export declare class Background extends ScreenElement {
    constructor(position: {
        x: number;
        y: number;
    });
    onInitialize(): void;
}
